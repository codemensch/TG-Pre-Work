/* Scope is where variables can be defined and referenced. Scope defines what
parts of a program can "see" variables. Because of scope, some variable are
local, can be seen by all parts of the program, others can only be seen by
parts of the program. */
