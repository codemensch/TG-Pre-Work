const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach( fruit => console.log(`I want to eat a ${fruit}`));

/*The .forEach iterator iterates over each array element and executes a callback
function passed as an argument on each array element. Any of the syntaxes for functions
can be used in defining the callback function.*/
