console.log('Teaching the world how to code'.length);

/* New data in a JavaScript program is saved as an "instance" of that data's
data type. Each instance has different properties associated with it. A string
instance has a property of 'length', which has stored the number of characters in
the string. Property infornation can be retrieved by appending the property name
to the data/string with a dot, e.g. "Hello".length. */
