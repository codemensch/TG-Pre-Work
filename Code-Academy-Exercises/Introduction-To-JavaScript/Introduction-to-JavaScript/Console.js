console.log(35);
console.log(364);

/* The console in terms of JavaScript is a window where messages and feedback
from the computer are displayed. In the JavaScript language, the "console"
keyword is an onject that contains data and methods, including the "console"
object, which contains the log() function. console.log() prints input given
to it to the console. */
