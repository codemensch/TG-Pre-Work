console.log(Math.floor(Math.random() * 100));
console.log(Math.ceil(43.8));
console.log(Number.isInteger(2017));

/* You can define your own objects but JavaScript also has some objects already 
pre-defined. One object is the Math object that has several mathematical methods
defined, like Math.random() which returns a random number between 0 and 1. */
