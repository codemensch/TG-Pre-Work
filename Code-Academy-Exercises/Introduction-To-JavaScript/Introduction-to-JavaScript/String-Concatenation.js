console.log('Hello ' + 'World' );

/* The + arithmetic operator can be used to join strings together, which is
called concatenation. Spaces have to be explicitly within the string. Multiple
strings ca be chained together using additional + operators.*/
