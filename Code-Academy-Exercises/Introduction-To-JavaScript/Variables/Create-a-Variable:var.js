var favoriteFood = 'pizza';
var numOfSlices = 8;
console.log(favoriteFood);
console.log(numOfSlices);

/*The original keyword for assigning a variable is 'var'. To assign a variable
using var one would write var, the name of the variable, the assignment operator
(=), and then a value (var myVariable = 6). Variable names must follow JavaScript
naming conventions.*/
