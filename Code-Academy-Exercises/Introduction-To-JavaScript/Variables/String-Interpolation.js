let myName = 'Cody';
let myCity = 'Charlottesville';
console.log(`My name is ${myName}. My favorite city is ${myCity}.`);

/*String interpolation is when the value of a variable is inserted into a string.
To do this we can use template literals which is backticks around the string (``)
and the variable placed in the placeholder ${myVariable}. Template literals
are easy to read and escaping quotes isn't necessary.*/
