let newVariable = 'Playing around with typeof.';
console.log(typeof newVariable);
newVariable = 1;
console.log(typeof newVariable);

/*You can check the data type of a variable by placing the typeof operator next to
the variable in question and logging the result. If you re-assign the variable to
a different data type, the result will be reflected when logged.*/
