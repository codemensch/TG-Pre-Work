class Surgeon {
  constructor(name, department) {
    this._name = name;
    this._department = department;
  }
}

/*The major difference between a class object and a normal object is the constructor.
The constructor method is called by JavaScript every time a new instance of a class is made.
constructor() sets the values of the class instance to the value of the arguments.*/
