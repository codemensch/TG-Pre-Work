// Write your code below
let vacationSpots = ['Paris', 'Brussels', 'Brighton'];
console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

/*Loops make it possible to not have to do repetitive, incremental tasks manually, like
logging all of the elements of a large array like above.*/
