const famousSayings = ['Fortune favors the brave.', 'A joke is a very serious thing.', 'Where there is love there is life.'];

let listItem = famousSayings[0];

console.log(listItem);

console.log(famousSayings[2]);

console.log(famousSayings[3]);

/*Arrays are zero indexed, meaning the first element of the array has an index
of 0. To access the value of a particular element by citing the name of the array
followed by square brackets with the index (number) of the element (array[2]). This
is bracket notation.*/
