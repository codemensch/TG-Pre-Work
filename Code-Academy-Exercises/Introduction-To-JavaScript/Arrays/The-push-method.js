const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('make bed', 'sweep chimney');
console.log(chores);

/*Arrays have built in methods as well. One method is the push() method that adds an
element to the end of the array. To use the method, use dot notation to chain the
method to the array. In the parentheses, list the elements to add to the array
and separate them with commas as seen above.*/
