let numberClusters = [[1, 2], [3, 4], [5, 6]];
const target = numberClusters[2][1];

/*Nested arrays are arrays in array elements. To access a specific element in a
nested array, we would use bracket notation, the first bracket representing the
index of the nested array, and the second representing the index of the specific
element of the nested array we want to access as seen above.*/
