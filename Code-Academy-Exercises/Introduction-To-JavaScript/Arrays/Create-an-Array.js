const hobbies = ['hiking', 'biking', 'triking'];
console.log(hobbies);

/*An array is created by using two square brackets (array literal) [] and expanding the array
with a list of items inside separated by commas. Arrays can be saved by assigning them
to variables. */
