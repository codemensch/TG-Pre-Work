const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/*Functions can also be defined by ommiting the function keyword and adding
the arrow symbol => pointing to the function body () => {}*/
