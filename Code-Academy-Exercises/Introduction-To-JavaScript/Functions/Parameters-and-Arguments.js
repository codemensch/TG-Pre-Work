function sayThanks(name) {
  console.log('Thank you for your purchase ' + name + '! We appreciate your business.');
}

sayThanks('Cole');

/*Functions can also take input from outside that affect its output. A function's
input is defined through its parameters which are placed between the parentheses.
The actual data that is passed through these parameters are called arguments and
are specified when the function is called.*/
