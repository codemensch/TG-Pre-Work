function makeShoppingList(item1 = 'milk', item2 = 'bread', item3 = 'eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}

/*If a function is defined with a parameter and no argument is passed, a default
argument can be defined that will pass to the function. This is done by using
the assignment operator (=) with the parameter name, like number = 25*/
