const plantNeedsWater = day => day === 'Wednesday' ? true : false;

/*Arrow functions can be further refactored into concise body arrow functions. If
there is zero parameters, parentheses are required, if only one, none are required,
if there are multiple they are required. Also, if the function is a single line,
curly braces can be omitted as well as the return statement, as value from the
function will be implicitly returned. */
