let sale = true;
sale = false;
if (sale){
  console.log('Time to buy!');
}

/*The if statement takes a condition inside parenthesis, if the condition is true,
the code inside the curly braces will run and not if it doesn't.*/
