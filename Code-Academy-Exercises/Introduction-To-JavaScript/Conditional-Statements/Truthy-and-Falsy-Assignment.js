let tool = 'marker';

// Use short circuit evaluation to assign  writingUtensil variable below:
let writingUtensil = tool || 'pen';

console.log(`The ${writingUtensil} is mightier than the sword.`);

/*Because data will have a truthy or falsy value, you can do a logical comparison
within variable assignment, this is called short circuit evaluation.*/
