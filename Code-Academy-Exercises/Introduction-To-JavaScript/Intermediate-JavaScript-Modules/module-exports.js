let Airplane = {};
Airplane.myAirplane = 'StarJet';
module.exports = Airplane;

/*Exporting a module requires creating an object that represents the module and using
module.exports to export the module. Properties can be added to the module by calling the
module and the new property name in dot notation and assigning a value
(Object.newProperty = value).*/
